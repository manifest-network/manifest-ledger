syntax = "proto3";
package liftedinit.sku.v1;

import "gogoproto/gogo.proto";
import "liftedinit/sku/v1/types.proto";

option go_package = "github.com/manifest-network/manifest-ledger/x/sku/types";

// GenesisState defines the sku module's genesis state.
message GenesisState {
  // params defines the module parameters.
  Params params = 1 [ (gogoproto.nullable) = false ];

  // providers is the list of providers.
  repeated Provider providers = 2 [ (gogoproto.nullable) = false ];

  // skus is the list of SKUs.
  repeated SKU skus = 3 [ (gogoproto.nullable) = false ];

  // next_provider_id is the next provider id to be assigned.
  uint64 next_provider_id = 4;

  // next_sku_id is the next SKU id to be assigned.
  uint64 next_sku_id = 5;
}
